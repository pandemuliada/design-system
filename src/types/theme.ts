export type IColor = 'default' | 'primary' | 'danger' | 'warning' | 'success' | 'muted'

export type IAvaliableColors = {
  default: string
  primary: string
  danger: string
  warning: string
  success: string
  muted: string
  [x: string]: string
}
